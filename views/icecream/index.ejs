<% include ../partials/header %>

<div class="container" ng-controller="IcecreamController">

  
  
  <div class="col-xs-4">

    <div class="panel panel-default panel-narrow" ng-repeat="icecream in icecreams">
      <div class="panel-heading">
        <h3 class="panel-title">{{icecream.flavor}}</h3>
      </div>
      <div class="panel-body">
        <img ng-src="{{icecream.imageUrl}}">
        <div>{{icecream.description}}</div>
        <!-- <a href="/icecreams/<%= el.id%>/edit"><button class="btn btn-default">Edit</button></a>
        <form action="/icecreams/<%= el.id%>?_method=delete" method="POST"><button class="btn btn-default">Delete</button></form> -->
      </div>
    </div>
  </div>
    <!-- <% }) %> -->

    
</div>




<% include ../partials/footer %>